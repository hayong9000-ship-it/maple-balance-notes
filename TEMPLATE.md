# 메이플 전 직업 밸런스 패치 고정 템플릿

이 프로젝트는 2026년 9월 10일 테스트월드 1.2.206을 기준으로 만든 재사용 원본입니다. 다음 패치 요청에는 현재 자료를 최신 자료처럼 재사용하지 말고, 해당 날짜의 공식 공지와 수정 이력을 새로 확인합니다.

## 고정할 구조

- 참고 사이트와 같은 밝은 바탕, 주황색 강조, 직업 초상화 목록, 공통 변경, 공지 정정 안내, 스킬별 전후 툴팁 구성.
- 9개 직업군: 모험가, 시그너스 기사단, 영웅, 레지스탕스, 마족, 노바, 레프, 아니마, 그 외.
- 마족: 데몬어벤져, 데몬슬레이어, 레테. 그 외: 제로, 키네시스. 아니마에 렌 포함.
- 모험가는 전사·마법사·궁수·도적·해적 하위 분류를 유지.
- 직업군 탭 → 직업 카드 → 해당 직업의 모든 스킬을 한 페이지에서 스크롤 → 비교 표·변경 설명·실제 툴팁 → 확대 보기.
- PC·모바일 모두 스킬별 선택 메뉴 없이 전체 내용을 연속 표시합니다. 모바일에서는 변경 전 → 변경 후 순서의 세로 배치를 유지합니다.
- 해당 직업 스킬을 공통 스킬보다 먼저 배치하고, 항목별 직접 링크는 해당 스킬 위치로 스크롤합니다. 모든 변경 설명을 접지 않고 표시합니다.

## 업데이트할 자료

실제 앱 원본은 `src/`입니다. `src/data.js`의 `window.PATCH_DATA`에서 다음 항목을 교체합니다.

- `meta`: 패치 번호, 날짜, 날짜 표시, 공식 공지 링크, 참고 자료 링크, 수정 시각, 수정 안내 목록, 공통 항목 요약.
- `groups`: 고정 직업군과 직업 ID. 신규 직업이 생기면 확인 후 정확한 그룹에 추가.
- `jobs`: 직업 ID, 이름, 초상화 경로, 그룹, 섹션, 변경 항목, 원문 링크.
- 각 섹션에는 `id`, 각 직업에는 첫 표시 항목 `defaultSkill`을 지정합니다. 각 스킬은 안정적인 `id`, `name`, 완결된 공식 변경 문장 `notes`, 전후를 확인한 `comparisons`, 실제 툴팁 `tips`를 가집니다. 같은 스킬을 합치면 이전 링크를 `aliases`에 보존합니다.
- 비교 행은 `label`, `before`, `after`, `kind`, 적용 조건 `condition`, 근거 문장의 위치 `noteIndex`를 기록합니다. 긴 기능 설명은 억지로 표에 줄이지 말고 변경 내용에 표시합니다.
- 툴팁: `src`, 실제 `width`, `height`, `alt`, `labels`, 상태 `status`, `before`와 `after`의 표시 영역 `{x,y,width,height}`. 없는 쪽은 `null`로 기록하고 신규·삭제 상태를 명시합니다.
- 전후 영역은 원본을 확인해서 지정합니다. 두 영역이 같은 폭이라고 가정하지 않습니다. 예를 들어 1.2.206 솔 헤카테 스틱스의 1924px 원본은 변경 후가 x=1494에서 시작합니다. 넓은 원본의 보조 표는 `supplementary`로 표시하고 전체 원본 보기에서 제공하며, 원본 이미지 파일은 보존합니다.
- 원문에 이름이 없는 툴팁은 `unnamed: true`로 별도 유지합니다. 임의로 앞 스킬에 붙이지 않습니다. 한 스킬의 여러 툴팁도 모두 세로로 이어서 표시합니다. 화면 밖 이미지만 지연 로딩합니다.
- 표시되는 `count`, `tooltipCount`는 교체한 자료를 바탕으로 다시 계산.

이전·이후 수치는 공식 공지에 모두 명시되어 있거나 실제 툴팁에서 확인했을 때만 비교 표에 입력합니다. 이전 수치가 없으면 변경 설명과 확인 가능한 툴팁으로 설명하고, 임의 값을 넣지 않습니다. 공지 정정 전후와 게임 패치 전후를 혼동하지 않습니다. 예: 데몬 임팩트 VI의 공지상 5→4 정정은 게임의 실제 이전 값이 5였음을 뜻하지 않습니다.

툴팁이 없는 항목에도 공식 변경 설명을 제공합니다. 툴팁 이미지로만 제공된 기타 변경 사항은 공식 문구인 것처럼 쓰지 않고 툴팁 자료로 표시합니다. 직접 게임 클라이언트에서 추출하지 않은 이미지를 직접 검증했다고 주장하지 않습니다.

## 저장과 확인

1. `src/`에 자료와 이미지를 넣고 `node scripts/build.mjs`로 `dist/`를 만듭니다.
2. `node scripts/validate.mjs`로 직업 누락·중복, 전후 표, 자료 경로, 빈 항목을 확인합니다.
3. 실제 브라우저에서 직업군·직업 이동, 정정 링크, 툴팁 확대·닫기, 모바일 화면을 확인합니다.
4. 공개 주소에는 `chatgpt`가 들어가지 않는 구성을 유지합니다. 별도 호스팅(Netlify 등)이나 사용자 소유 도메인을 사용합니다. 현재 호스팅을 확인하고 해당 사이트를 업데이트하며, 새 사이트에는 이전 `project_id`를 재사용하지 않습니다. Sites 사용자 도메인을 쓰는 경우에는 Sites 제작·게시 절차를 따릅니다.

## 기준 자료

- 공식: https://maplestory.nexon.com/testworld/news/all/198
- 참고 화면·직업 자료·툴팁: https://jsmm-00.github.io/MapleStory-Balance-Patch/index.html
- 반영 시점: 2026.09.11 13:55 공지 수정분.
- 본 템플릿은 NEXON 공식 사이트가 아닙니다. 게임 및 툴팁 이미지는 원 권리자에게 귀속됩니다.
